import React from "react";
import { Button } from "../ui/button";

const Rejected = () => {
  return (
    <Button className=" h-fit w-12 cursor-default rounded-lg bg-red-400 text-xs font-semibold text-red-800 md:w-20">
      Rejected
    </Button>
  );
};

export default Rejected;
