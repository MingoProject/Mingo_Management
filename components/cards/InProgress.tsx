import React from "react";
import { Button } from "../ui/button";

const InProgress = () => {
  return (
    <Button className="h-fit w-12 cursor-default  rounded-lg  bg-yellow-300  text-xs font-semibold text-yellow-800 md:w-20">
      In progress
    </Button>
  );
};

export default InProgress;
